# import math_operators #
from math_operators import *

first_num = int(input("type the first number : "))
second_num = int(input("type the second number :"))
result = add(first_num, second_num)
print(f"{first_num} + {second_num} = {result}")


# A package is multiple modules bundled into one

# Library = catch all term. Tends to be useful for large packages
