# Build a calculator that takes two numbers then adds, sub-tacks, minus and divides them based on user request

# import math_operators

from math_operators import *

# define and build the actual calculator

num_1 = float(input("type the first number"))
num_2 = float(input("type the second number"))

# Define the operators below

result = (add(num_1, num_2)

print(f"{num_1} {num_2}")
